[package]
name = "packet-browser-shared"
version = "0.1.1"
edition = "2021"
description = "Shared protocol types and compression helpers for packet browser"
license = "GPL-3.0"

[dependencies]
base64 = "0.22"
brotli = "8"
thiserror = "1"
